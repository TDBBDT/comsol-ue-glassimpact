# 模型保存位置

此目录尚无已经求解的玻璃模型。按 `docs/comsol_model_guide.md` 在 GUI 完成模块检查和建模后，将输入模型另存为 `GlassImpact_A1_input.mph`，求解模型另存新文件。保留参数、日志、网格/时间与能量验证记录。

参数和导出适配器可复用，但 Python synthetic 样例不构成 `.mph` 结果。不要从土体工程改名生成玻璃结果。
